Read AGENTS.md first and follow it strictly.

Implement only the custom bottom tab navigation for the (tabs) layout based on the provided design reference.

Please perform the following:
Create the folder structure and basic placeholder screens for Today (Home), Community, Progress, and Profile. Build a custom tab bar component in components/navigation/TabBar.tsx (or similar). Implement a smooth animated transition for an active circular indicator. The active tab should show a circular background with only the icon. Show the icon and label. Use NativeWind for all layout and component styling. Use StyleSheet only if specific animation or platform-related needs (like Animated.View) cannot be handled by NativeWind classes.

Focus only on the navigation structure and the custom tab bar UI. Do not build the Home screen content yet. Ensure the transition animation is smooth and performant. Prioritize clean, readable code and maintainable component patterns as per the project philosophy.